# =============================================================================
# File Name   : harness/mcp/file_organizer_server.py
# Purpose     : 예전 이름으로도 파일 정리 MCP를 열 수 있게 돕는 연결 파일
# =============================================================================
# CHANGE HISTORY
# =============================================================================
# 20260902 | Codex | Harness 표준 파일 헤더와 호환 진입점 역할을 보강했음
# =============================================================================
# 이 파일은 무엇을 하나요?
# - 예전에 이 파일을 사용하던 설정이 깨지지 않도록 새 서버를 연결합니다.
# - 실제 파일 정리 기능은 mcp_harness_server.py에 있습니다.
# 주의: 이 파일에는 기능을 새로 만들지 않고 연결 역할만 둡니다.
"""Compatibility import for the relocated file-organizer Harness server."""

from harness.mcp.mcp_harness_server import mcp


if __name__ == "__main__":
    mcp.run()
